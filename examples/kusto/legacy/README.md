# Legacy Kusto example

These files preserve the combined Kusto example immediately before
`c45913998aba22032bf84101c7d5fbe7796adda2` split it into Data and Ingest
runners.

They retain historical imports and are intentionally excluded from all build
roots. Use the maintained examples in `data/`, `ingest/`, or `runner.zig`.
