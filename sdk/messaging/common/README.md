# azure_sdk_messaging_common

Shared messaging types used by Event Hubs and Service Bus.

Release branch: `sdk/messaging_common`. The package has no internal
dependencies and starts at `0.1.0`.

The current file-rooted module moves from `sdk/messaging/common.zig` to this
directory when the package is extracted.
