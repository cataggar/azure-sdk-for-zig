# azure_sdk_data_tables

Azure Tables clients:

- `TableClient`
- `TableServiceClient`

Release branch: `sdk/data_tables`. The package depends on
`azure_sdk_core` and starts at `0.1.0`.

## Development

```bash
zig build test --summary all
```
